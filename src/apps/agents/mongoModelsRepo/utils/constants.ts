import { jsonContentType } from '../../utils/constants';

export const COLLECTION_NAME = 'agentProfiles';
export const JSON_OBJECT_FILTER = {
  contentType: jsonContentType,
  isObjectContent: true,
};
